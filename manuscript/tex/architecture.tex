\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{fit, positioning, arrows.meta, calc}

\begin{document}
\setlength{\baselineskip}{1.0em}

\begin{tikzpicture}[
        box/.style={draw, rounded corners, minimum width=6em, minimum height=1em, align=center, fill=white, scale=0.7},
        fitbox/.style={draw, rounded corners, dashed, inner sep=0.5em},
        arrow/.style={-Stealth, thick},
        % label/.style={font=\bfseries\large}
    ]

    % Nodes for Developer API
    \node[box] (priors) {\texttt{treeflow.distributions.tree} \\ \small{Tree priors}};
    \node[box, below=1em of priors.south, anchor=north] (evolution) {\texttt{treeflow.evolution} \\ \small{Substitution models}};
    \node[box, right=1em of priors.east, anchor=west] (io) {\texttt{treeflow.tree.io} \\ \small{Tree reading and writing}};
    \node[box, above=1em of io.north, anchor=south] (tree) {\texttt{treeflow.tree} \\ \small{Tree representation}};
    \node[box, right=1em of tree.east, anchor=west] (traversal) {\texttt{treeflow.traversal} \\ \small{Tree traversals}};
    \node[box, right=1em of io.east, anchor=west] (bijectors) {\texttt{treeflow.bijectors} \\ \small{Bijections on times on trees}};
    \node[box, below=1em of bijectors.south, anchor=north] (approximation) {\texttt{treeflow.model.approximation} \\ \small{Tree posterior approximations}};
    \node[box, below=1em of approximation.south, anchor=north] (vi) {\texttt{treeflow.vi} \\ \small{Fixed topology variational inference}};

    % Fitting box for Developer API
    \node[fitbox, fit=(priors)(tree)(evolution)(io)(traversal)(bijectors)(approximation)(vi)] (developerapi) {};
    \node[label, above=0.5em of developerapi] {Developer API};

    % Nodes for User Interface
    \node[box, below=4em of developerapi.south, anchor=north, xshift=-4.5em] (phylomodel) {\texttt{treeflow.model.phylo\_model} \\ \small{Model specification}};
    \node[box, right=2em of phylomodel] (cli) {\texttt{treeflow.cli} \\ \small{Inference command line interfaces}};

    % Fitting box for User Interface
    \node[fitbox, fit=(cli)(phylomodel)] (userinterface) {};
    \node[label, above=0.5em of userinterface] {User interface};

    % TensorFlow and TensorFlow Probability nodes
    \node[box, above=1em of traversal.north, anchor=south] (tensorarray) {\texttt{tensorflow.TensorArray}};
    \node[box, below=1em of phylomodel.south, anchor=north] (tfp) {\texttt{tensorflow\_probability.distributions} \\ \small{TensorFlow Probability Distributions}};

    % Connections
    \draw[arrow] (tree) -- (priors);
    \draw[arrow] (priors) |- ($ (priors) !.5! (evolution) $) -| (phylomodel.north);
    \draw[arrow] (tree) -- (io);
    \draw[arrow] (tree) -- (traversal);
    \draw[arrow] (traversal) -- (bijectors);
    \draw[arrow] (bijectors) -- (approximation);
    \draw[arrow] (approximation) -- (vi);
    \draw[arrow] (io) |- ([yshift=-0.5em,xshift=2em]developerapi.south) -| (cli);
    \draw[arrow] (evolution) -- ([xshift=-2em]phylomodel.north);

    \draw[arrow] ([xshift=3em]vi.south) -- ([xshift=3em]cli.north);
    \draw[arrow] (phylomodel) -- (cli);

    \draw[arrow] (tensorarray) -- (traversal);
    \draw[arrow] (tfp) -- (phylomodel);

\end{tikzpicture}

\end{document}
